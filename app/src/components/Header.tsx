import { Typography } from "@mui/material";

const Header = () => {
    return (
        <div style={{backgroundColor: "gainsboro", textAlign: "center"}}>
            <Typography sx={{fontSize: 64, fontWeight: "500"}}>Biggity Block</Typography>
        </div>
    )
}

export default Header;